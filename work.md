2026-06-28 09:13:57 +08:00 --- 发现 Electron 开发启动时渲染服务只监听 IPv6 回环地址，Electron 访问 localhost:5173 出现 ERR_CONNECTION_REFUSED；同时当前终端存在 ELECTRON_RUN_AS_NODE=1 会导致 Electron API 为空 --- 通过启动时移除 ELECTRON_RUN_AS_NODE，并在 electron.vite.config.ts 中将 renderer.server.host 固定为 127.0.0.1 解决本机地址族不一致问题 --- 修改了 electron.vite.config.ts、work.md --- 撤回方式：删除 electron.vite.config.ts 中 renderer.server.host 配置，并删除本条 work.md 记录。
2026-06-28 11:46:05 +08:00 --- 发现 Phase 1-3 需要 SQLite 初始化和时间线核心逻辑，同时当前项目缺少 better-sqlite3 依赖；初次按外部计划迁移到 electron/main 与 electron/preload 后，需要同步调整 electron-vite、TypeScript、package main、打包配置和 pnpm 原生模块构建许可 --- 新增 better-sqlite3 与 @types/better-sqlite3，允许 better-sqlite3 原生构建；新增 db.ts 建立 timeline-journal.db 并创建 time_markers、time_segments、app_settings；新增 timeline.ts 实现 markNow(source) 与 getTodaySegments()；迁移主进程和预加载入口到 electron 目录；修正 pnpm 脚本、preload .mjs 路径、React 自动 JSX lint 配置，并通过 pnpm run typecheck、vp check、pnpm run build 验证，vp test 因无测试文件返回 No test files found --- 修改了 package.json、pnpm-lock.yaml、pnpm-workspace.yaml、electron.vite.config.ts、electron-builder.yml、tsconfig.node.json、tsconfig.web.json、vite.config.ts、electron/main/index.ts、electron/main/db.ts、electron/main/timeline.ts、electron/preload/index.ts、electron/preload/index.d.ts，删除了 src/main/index.ts、src/preload/index.ts、src/preload/index.d.ts --- 撤回方式：将 electron/main 与 electron/preload 内容迁回 src/main 与 src/preload 或用 git 恢复上述文件；删除 pnpm-workspace.yaml 中 better-sqlite3 构建许可；从 package.json 和 pnpm-lock.yaml 移除 better-sqlite3、@types/better-sqlite3 及脚本调整；恢复 electron.vite.config.ts、electron-builder.yml、tsconfig.node.json、tsconfig.web.json、vite.config.ts、package.json main 到修改前状态；删除本条 work.md 记录。
2026-06-28 11:52:56 +08:00 --- 发现顶层 electron/main 与 electron/preload 不是 electron-vite 脚手架默认约定，且恢复默认 src/main 与 src/preload 后构建产物重新变为 out/main/index.js 与 out/preload/index.js --- 将 db.ts、timeline.ts、index.ts、preload index.ts 和 index.d.ts 迁回 src/main 与 src/preload；删除顶层 electron 空目录；恢复 electron.vite.config.ts 为 main/preload 默认自动发现；恢复 tsconfig.node.json、tsconfig.web.json 指向 src；移除 electron-builder.yml 中对 electron 源目录的排除项；将 package.json main 与 BrowserWindow preload 路径改回 .js；通过 pnpm run typecheck、vp check、pnpm run build 验证，vp test 因无测试文件返回 No test files found --- 修改了 package.json、electron.vite.config.ts、electron-builder.yml、tsconfig.node.json、tsconfig.web.json、src/main/index.ts、src/main/db.ts、src/main/timeline.ts、src/preload/index.ts、src/preload/index.d.ts、work.md；删除了 electron 目录 --- 撤回方式：如需回到自定义 electron 目录结构，可将 src/main 与 src/preload 对应文件迁回 electron/main 与 electron/preload，恢复 electron.vite.config.ts 的 rollupOptions.input，恢复 tsconfig include 与 electron-builder.yml 排除项，将 package.json main 和 preload 路径按实际构建产物调整，并删除本条 work.md 记录。
2026-06-28 12:01:37 +08:00 --- 发现开发启动时存在两个问题：better-sqlite3 原生模块可能按本机 Node ABI 137 编译而 Electron 39.8.10 需要 ABI 140；同时当前终端存在 ELECTRON_RUN_AS_NODE=1 会让 Electron 以 Node 模式启动，导致 electron.app 与 preload 暴露的 window.electron 为空；主进程初始化异常还会形成 UnhandledPromiseRejectionWarning --- 通过 pnpm run rebuild:native 调用 electron-builder install-app-deps 让 better-sqlite3 按 Electron ABI 重建；新增 scripts/run-electron-vite.mjs，在 dev/start 调用 electron-vite 前删除 ELECTRON_RUN_AS_NODE；为 app.whenReady() 增加 catch 记录启动失败并退出；通过 Electron ABI require better-sqlite3、pnpm run typecheck、vp check、pnpm run build，以及带 ELECTRON_RUN_AS_NODE=1 的短时 pnpm run dev 日志验证 --- 修改了 package.json、scripts/run-electron-vite.mjs、src/main/index.ts、work.md，并因原生重建更新了 node_modules 本地构建产物 --- 撤回方式：将 package.json 的 dev/start 改回 electron-vite dev/preview，删除 rebuild:native 脚本和 scripts/run-electron-vite.mjs，移除 src/main/index.ts 中 app.whenReady().catch 分支；如需撤回原生重建，可删除 node_modules 后重新 vp install；删除本条 work.md 记录。
2026-06-28 13:10:19 +08:00 --- 发现后续开发人员需要理解主进程、数据库、时间线事务、preload 安全暴露和启动包装脚本的关键设计意图 --- 为核心边界补充中文注释，解释数据库 userData 路径、WAL 与外键设置、幂等建表、本地日边界、坏状态降级、markNow 事务原子性、BrowserWindow preload 安全入口、外链处理、contextBridge 暴露约束、ELECTRON_RUN_AS_NODE 清理原因和 Windows .cmd shim 避让；通过 pnpm run typecheck 与 vp check 验证 --- 修改了 src/main/db.ts、src/main/timeline.ts、src/main/index.ts、src/preload/index.ts、src/preload/index.d.ts、scripts/run-electron-vite.mjs、work.md --- 撤回方式：删除上述文件中本次新增的中文注释，并删除本条 work.md 记录。

2026-06-28 09:13:57 +08:00 --- 发现 Electron 开发启动时渲染服务只监听 IPv6 回环地址，Electron 访问 localhost:5173 出现 ERR_CONNECTION_REFUSED；同时当前终端存在 ELECTRON_RUN_AS_NODE=1 会导致 Electron API 为空 --- 通过启动时移除 ELECTRON_RUN_AS_NODE，并在 electron.vite.config.ts 中将 renderer.server.host 固定为 127.0.0.1 解决本机地址族不一致问题 --- 修改了 electron.vite.config.ts、work.md --- 撤回方式：删除 electron.vite.config.ts 中 renderer.server.host 配置，并删除本条 work.md 记录。
2026-06-28 11:46:05 +08:00 --- 发现 Phase 1-3 需要 SQLite 初始化和时间线核心逻辑，同时当前项目缺少 better-sqlite3 依赖；初次按外部计划迁移到 electron/main 与 electron/preload 后，需要同步调整 electron-vite、TypeScript、package main、打包配置和 pnpm 原生模块构建许可 --- 新增 better-sqlite3 与 @types/better-sqlite3，允许 better-sqlite3 原生构建；新增 db.ts 建立 timeline-journal.db 并创建 time_markers、time_segments、app_settings；新增 timeline.ts 实现 markNow(source) 与 getTodaySegments()；迁移主进程和预加载入口到 electron 目录；修正 pnpm 脚本、preload .mjs 路径、React 自动 JSX lint 配置，并通过 pnpm run typecheck、vp check、pnpm run build 验证，vp test 因无测试文件返回 No test files found --- 修改了 package.json、pnpm-lock.yaml、pnpm-workspace.yaml、electron.vite.config.ts、electron-builder.yml、tsconfig.node.json、tsconfig.web.json、vite.config.ts、electron/main/index.ts、electron/main/db.ts、electron/main/timeline.ts、electron/preload/index.ts、electron/preload/index.d.ts，删除了 src/main/index.ts、src/preload/index.ts、src/preload/index.d.ts --- 撤回方式：将 electron/main 与 electron/preload 内容迁回 src/main 与 src/preload 或用 git 恢复上述文件；删除 pnpm-workspace.yaml 中 better-sqlite3 构建许可；从 package.json 和 pnpm-lock.yaml 移除 better-sqlite3、@types/better-sqlite3 及脚本调整；恢复 electron.vite.config.ts、electron-builder.yml、tsconfig.node.json、tsconfig.web.json、vite.config.ts、package.json main 到修改前状态；删除本条 work.md 记录。
2026-06-28 11:52:56 +08:00 --- 发现顶层 electron/main 与 electron/preload 不是 electron-vite 脚手架默认约定，且恢复默认 src/main 与 src/preload 后构建产物重新变为 out/main/index.js 与 out/preload/index.js --- 将 db.ts、timeline.ts、index.ts、preload index.ts 和 index.d.ts 迁回 src/main 与 src/preload；删除顶层 electron 空目录；恢复 electron.vite.config.ts 为 main/preload 默认自动发现；恢复 tsconfig.node.json、tsconfig.web.json 指向 src；移除 electron-builder.yml 中对 electron 源目录的排除项；将 package.json main 与 BrowserWindow preload 路径改回 .js；通过 pnpm run typecheck、vp check、pnpm run build 验证，vp test 因无测试文件返回 No test files found --- 修改了 package.json、electron.vite.config.ts、electron-builder.yml、tsconfig.node.json、tsconfig.web.json、src/main/index.ts、src/main/db.ts、src/main/timeline.ts、src/preload/index.ts、src/preload/index.d.ts、work.md；删除了 electron 目录 --- 撤回方式：如需回到自定义 electron 目录结构，可将 src/main 与 src/preload 对应文件迁回 electron/main 与 electron/preload，恢复 electron.vite.config.ts 的 rollupOptions.input，恢复 tsconfig include 与 electron-builder.yml 排除项，将 package.json main 和 preload 路径按实际构建产物调整，并删除本条 work.md 记录。
2026-06-29 11:25:00 +08:00 --- 发现Phase 6系统托盘与Phase 7窗口隐藏退出需要完整的解耦架构、防抖闪烁动画与全局单例生命周期保证，同时需避免在单测中直接依赖Electron原生的C++绑定 --- 生成32x32标准与高亮托盘图标资源；新建src/main/tray.ts与TrayManager，解耦实现500ms重置定时器的动画防抖；在src/main/index.ts中完成setupTray初始化与before-quit显式销毁，同时拦截window close事件实现隐藏到托盘；新建src/main/tray.test.ts编写Fake Adapter单测；通过vp test（5项通过）、vp check（0警告0报错）验证 --- 修改了resources/tray-normal.png、resources/tray-marked.png、src/main/tray.ts、src/main/tray.test.ts、src/main/index.ts、work.md --- 撤回方式：删除resources/tray-normal.png、resources/tray-marked.png、src/main/tray.ts与src/main/tray.test.ts；恢复src/main/index.ts至修改前状态；删除本条work.md记录。
2026-06-28 12:01:37 +08:00 --- 发现开发启动时存在两个问题：better-sqlite3 原生模块可能按本机 Node ABI 137 编译而 Electron 39.8.10 需要 ABI 140；同时当前终端存在 ELECTRON_RUN_AS_NODE=1 会让 Electron 以 Node 模式启动，导致 electron.app 与 preload 暴露的 window.electron 为空；主进程初始化异常还会形成 UnhandledPromiseRejectionWarning --- 通过 pnpm run rebuild:native 调用 electron-builder install-app-deps 让 better-sqlite3 按 Electron ABI 重建；新增 scripts/run-electron-vite.mjs，在 dev/start 调用 electron-vite 前删除 ELECTRON_RUN_AS_NODE；为 app.whenReady() 增加 catch 记录启动失败并退出；通过 Electron ABI require better-sqlite3、pnpm run typecheck、vp check、pnpm run build，以及带 ELECTRON_RUN_AS_NODE=1 的短时 pnpm run dev 日志验证 --- 修改了 package.json、scripts/run-electron-vite.mjs、src/main/index.ts、work.md，并因原生重建更新了 node_modules 本地构建产物 --- 撤回方式：将 package.json 的 dev/start 改回 electron-vite dev/preview，删除 rebuild:native 脚本和 scripts/run-electron-vite.mjs，移除 src/main/index.ts 中 app.whenReady().catch 分支；如需撤回原生重建，可删除 node_modules 后重新 vp install；删除本条 work.md 记录。
2026-06-28 13:10:19 +08:00 --- 发现后续开发人员需要理解主进程、数据库、时间线事务、preload 安全暴露和启动包装脚本的关键设计意图 --- 为核心边界补充中文注释，解释数据库 userData 路径、WAL 与外键设置、幂等建表、本地日边界、坏状态降级、markNow 事务原子性、BrowserWindow preload 安全入口、外链处理、contextBridge 暴露约束、ELECTRON_RUN_AS_NODE 清理原因和 Windows .cmd shim 避让；通过 pnpm run typecheck 与 vp check 验证 --- 修改了 src/main/db.ts、src/main/timeline.ts、src/main/index.ts、src/preload/index.ts、src/preload/index.d.ts、scripts/run-electron-vite.mjs、work.md --- 撤回方式：删除上述文件中本次新增的中文注释，并删除本条 work.md 记录。
2026-06-28 14:32:18 +08:00 --- 发现 Phase 4 需要让渲染进程通过受控 preload API 调用主进程时间线能力，但当前只需要请求响应式 IPC，不需要 timeline:updated 事件推送 --- 新增共享 timeline 类型，新增 src/main/ipc.ts 注册 timeline:mark-now 与 timeline:get-today-segments；新增 src/preload/timeline-api.ts，通过 contextBridge 暴露 window.timelineAPI.markNow() 与 window.timelineAPI.getTodaySegments()；在主进程启动时注册 IPC；更新 preload 类型声明和 tsconfig include；通过 pnpm run typecheck、vp check、pnpm run build 以及构建产物通道名扫描验证 --- 修改了 src/shared/timeline.ts、src/main/ipc.ts、src/main/timeline.ts、src/main/index.ts、src/preload/timeline-api.ts、src/preload/index.ts、src/preload/index.d.ts、tsconfig.node.json、tsconfig.web.json、work.md --- 撤回方式：删除 src/shared/timeline.ts、src/main/ipc.ts、src/preload/timeline-api.ts；从 src/main/index.ts 移除 registerTimelineIpc 调用；从 src/main/timeline.ts 恢复本地类型定义；从 src/preload/index.ts 和 index.d.ts 移除 timelineAPI 暴露与声明；恢复 tsconfig.node.json、tsconfig.web.json 的 include；删除本条 work.md 记录。
2026-06-28 14:39:14 +08:00 --- 发现需要先用最小垂直切片验证 React 到 preload、IPC、主进程、SQLite 的链路，而不是直接进入正式今日列表 UI --- 将默认 electron-vite 模板 App 替换为最小 IPC 调试界面，提供 Mark Now、Refresh、错误提示、今日时间段列表和 JSON 预览；重写 main.css 为调试工具布局；通过 pnpm run typecheck、vp check、pnpm run build，以及短时 pnpm run dev 日志验证主进程、preload、renderer 启动无 stderr 报错 --- 修改了 src/renderer/src/App.tsx、src/renderer/src/assets/main.css、work.md --- 撤回方式：恢复 App.tsx 与 main.css 到脚手架模板或后续正式 UI 版本，并删除本条 work.md 记录。
2026-06-28 15:18:30 +08:00 --- 发现 markNow 核心行为需要 Vitest 回归测试，但直接在 Node 测试环境加载 better-sqlite3 会与当前 Electron ABI 140 原生构建冲突，而 Node Vitest 需要 ABI 137 --- 将时间线核心逻辑拆到 src/main/timeline-service.ts，并定义最小 TimelineDatabase 端口；将表结构 SQL 拆到 src/main/db-schema.ts 供运行时代码复用；src/main/timeline.ts 保持原 markNow/getTodaySegments 对外 API；新增 src/main/timeline-service.test.ts，使用 Vite+ 推荐的 vite-plus/test 和内存 fake 数据库验证第一次 markNow 创建 active segment、第二次 markNow 关闭旧 active segment 并创建新 active segment；确认不需要直接添加 vitest 依赖，使用 Vite+ 自带测试入口；通过 vp test、pnpm run typecheck、vp check、pnpm run build 验证 --- 修改了 src/main/db.ts、src/main/db-schema.ts、src/main/timeline.ts、src/main/timeline-service.ts、src/main/timeline-service.test.ts、work.md --- 撤回方式：删除 src/main/db-schema.ts、src/main/timeline-service.ts、src/main/timeline-service.test.ts；把 schema SQL 和 markNow/getTodaySegments 逻辑恢复回 src/main/db.ts 与 src/main/timeline.ts；删除本条 work.md 记录。
2026-06-28 15:34:19 +08:00 --- 发现最小 IPC 调试界面已经验证链路，需要升级为 V0.1 正式今日时间段 UI，同时保留 Mark Now 与 Refresh 方便后续验证快捷键和托盘 --- 新增 Header、TodaySegmentList、SegmentItem 三个渲染组件；App.tsx 改为负责加载、刷新、错误状态和组合组件；SegmentItem 负责本地时间、持续时间、状态文案格式化；main.css 从调试布局改为正式工具型时间线布局，支持桌面表格和移动端单列显示；删除脚手架 Versions 组件；通过 vp test、pnpm run typecheck、vp check、pnpm run build 验证 --- 修改了 src/renderer/src/App.tsx、src/renderer/src/assets/main.css、src/renderer/src/components/Header.tsx、src/renderer/src/components/TodaySegmentList.tsx、src/renderer/src/components/SegmentItem.tsx、work.md，删除了 src/renderer/src/components/Versions.tsx --- 撤回方式：删除新增的 Header、TodaySegmentList、SegmentItem，恢复 App.tsx 与 main.css 到上一版 IPC Debug 界面，恢复 Versions.tsx，删除本条 work.md 记录。
2026-06-28 15:41:23 +08:00 --- 发现 V0.1 需要全局快捷键触发 markNow 后让已打开的今日时间线自动刷新 --- 新增 src/main/shortcut.ts 注册 Control+Alt+Space，触发后调用 markNow('hotkey') 并发送 timeline:updated；扩展 src/main/ipc.ts 增加 timeline:updated 通知通道和 notifyTimelineUpdated；扩展 src/preload/timeline-api.ts 与 src/shared/timeline.ts，暴露 window.timelineAPI.onTimelineUpdated(callback) 并返回取消监听函数；src/renderer/src/App.tsx 订阅 timeline:updated 后自动重新加载今日时间段；src/main/index.ts 在应用 ready 后注册快捷键并在 will-quit 注销，Linux 下启用 GlobalShortcutsPortal；通过 vp test、pnpm run typecheck、vp check、pnpm run build 和短时 pnpm run dev 验证，短时 dev 仅出现并发开发实例导致的 Chromium GPU cache 访问拒绝日志 --- 修改了 src/main/shortcut.ts、src/main/index.ts、src/main/ipc.ts、src/preload/timeline-api.ts、src/shared/timeline.ts、src/renderer/src/App.tsx、work.md --- 撤回方式：删除 src/main/shortcut.ts；从 src/main/index.ts 移除快捷键注册、注销和 Linux GlobalShortcutsPortal；从 src/main/ipc.ts 移除 timeline:updated 与 notifyTimelineUpdated；从 src/preload/timeline-api.ts、src/shared/timeline.ts 和 src/renderer/src/App.tsx 移除 onTimelineUpdated 暴露和订阅；删除本条 work.md 记录。
2026-06-29 10:38:00 +08:00 --- 发现 src/main/timeline.ts 属于浅层模块 [Shallow Module]，导致主进程多出无意义的中转转接，且快捷键响应无法自动内聚通知逻辑 --- 删除 timeline.ts 浅层中转文件；在 timeline-service.ts 中注入 onUpdated 回调并内聚自动通知；在组合根 index.ts 中直接依赖注入 TimelineService 实例至 IPC 和 Shortcut 模块；并在 timeline-service.test.ts 中为 onUpdated 回调增加单元测试用例通过 vp test 和 typecheck 验证 --- 修改了 src/main/timeline-service.ts、src/main/timeline-service.test.ts、src/main/ipc.ts、src/main/shortcut.ts、src/main/index.ts、work.md，删除了 src/main/timeline.ts --- 撤回方式：用 git 恢复 src/main/timeline.ts 或重新创建该文件，并恢复上述修改过的文件至重构前状态，删除本条 work.md 记录。
2026-06-29 11:30:00 +08:00 --- 发现点击系统托盘图标时因之前校验了 mainWindow.isFocused()，由于点击托盘瞬间鼠标焦点移出主窗口导致 isFocused() 恒为 false，从而误入 show() 分支无法隐藏窗口 --- 提取 WindowAdapter 与 toggleWindowVisibility 纯逻辑函数，将显隐切换条件修正为只需校验 isVisible() && !isMinimized()；在 tray.test.ts 中新增针对非 focus 状态点击托盘的回归测试用例；通过 vp test（6项全部通过）、vp check 验证 --- 修改了 src/main/tray.ts、src/main/tray.test.ts、work.md --- 撤回方式：恢复 src/main/tray.ts 中 tray.on('click') 的旧条件判断；删除 tray.test.ts 中的新增测试；删除本条 work.md 记录。
2026-06-29 11:37:00 +08:00 --- 发现 Windows 操作系统下调用 setContextMenu 会静态拦截托盘鼠标左键 click 事件，导致左键点击无法正常切换窗口显隐 --- 移除静态 setContextMenu 绑定，改用监听 right-click 事件动态触发 popUpContextMenu(contextMenu)，彻底实现 Windows 下左键 click 执行显隐切换、右键 right-click 弹出菜单的完全解耦；通过 vp test 与 vp check 验证 --- 修改了 src/main/tray.ts、work.md --- 撤回方式：恢复 src/main/tray.ts 中 setContextMenu 静态绑定并移除 right-click 监听；删除本条 work.md 记录。
2026-06-29 13:47:00 +08:00 --- 发现 Phase 9 需要实现基础编辑能力（时间段标题修改与状态更新为 edited），需贯通数据库、主进程服务、IPC、Preload 及前端交互组件 --- 在 timeline-service.ts 中增加 updateSegmentTitle，实现修改标题、更新时间与状态非"待整理"时自动更名为 edited 逻辑；在 timeline-service.test.ts 中为 Fake 库补齐匹配并编写 TDD 测试；扩展 ipc.ts 通道 timeline:update-segment-title 与 preload 暴露；在 React 渲染组件 SegmentItem 中实现点击标题内联编辑、Enter/Blur 保存、Esc 撤销，并补全 main.css hover 交互样式；通过 vp test（13项全过）与 vp check（0警告0错误）验证 --- 修改了 src/shared/timeline.ts、src/main/timeline-service.ts、src/main/timeline-service.test.ts、src/main/ipc.ts、src/preload/timeline-api.ts、src/renderer/src/components/SegmentItem.tsx、src/renderer/src/components/TodaySegmentList.tsx、src/renderer/src/App.tsx、src/renderer/src/assets/main.css、work.md --- 撤回方式：用 git 恢复上述修改过的文件至修改前状态；删除本条 work.md 记录。
